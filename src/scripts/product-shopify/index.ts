// import '../product-wp/amazon'
import 'dotenv/config' // required for env
import shopify from './shopify'

setTimeout(() => {

  return shopify()
  // getProducts().then(data => {
  //   return shopify(data)
  // }).catch(e => console.error(e))
}, 0)
